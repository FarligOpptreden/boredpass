form#add_listing_1.active
  p Do you know of a business or venue that provides fun or interesting activities? Of course you do! Help cure boredom and list it on BoredPass today for free. All you need to do is complete our simple 3 part form. If you are unsure about anything, please check out our FAQ page. 
  hr
  .field-wrapper
    label(for='businessname') Name of business/venue*
    input#businessname.mandatory(type='text', autocomplete='new-businessname')
  .tag-field
    .field-wrapper
      - var tagValues = activity && activity.tags && activity.tags.length ? activity.tags.join(';') : null
      label(for='activity_tags') Search for activity tags (e.g. Kayaking)*
      input#activity_tags.activity-tags.mandatory(type='text', data-value=tagValues, autocomplete='new-activity_tags')
    .tag-container
      if activity && activity.tags && activity.tags.length
        each tag in activity.tags
          span.tag.icon.activity.colour(class=tag.icon, data-tag=tag)=tag.name
  .field-wrapper
    label(for='address') Address*
    input#address.mandatory(type='text', autocomplete='new-address')
  .field-wrapper
    label(for='website') Website link
    input#website(type='text', autocomplete='new-website')
  .toggle-wrapper
    label Is this your business?*
    input#yourbusiness.mandatory(type='hidden', autocomplete='new-yourbusiness')
    .options
      button.active Yes
      button No
  .button-wrapper
    button.button.dark.next Next